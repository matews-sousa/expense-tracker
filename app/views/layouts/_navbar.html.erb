<nav class="flex items-center justify-between px-4 md:px-10 py-6">
  <%= link_to 'Finances', root_path, class: 'text-3xl font-bold text-green-600' %>
  <ul class="hidden md:flex items-center space-x-4">
    <li>
      <%= link_to 'Dashboard', root_path, class: "px-4 py-3 rounded-md text-gray-500 font-medium #{'text-white bg-gray-700' if current_page?(root_path)}" %>
    </li>
    <li>
      <%= link_to 'Categories', categories_path, class: "px-4 py-3 rounded-md text-gray-500 font-medium #{'text-white bg-gray-700' if current_page?(categories_path)}" %>
    </li>
    <li>
      <%= link_to 'Transactions', transactions_path, class: "px-4 py-3 rounded-md text-gray-500 font-medium #{'text-white bg-gray-700' if current_page?(transactions_path)}" %>
    </li>
  </ul>
  <% if current_user %>
    <div>
      <%= button_to 'Sign Out', destroy_user_session_path, :method => :delete, class: 'btn btn-error' %>
    </div>
  <% end %>
</nav>
